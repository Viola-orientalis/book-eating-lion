<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookeatinglion.api.mapper.BookMapper">

    <sql id="bookConditions">
        <where>
            sale_status = 'ON_SALE'
            <if test="keyword != null and keyword != ''">
                AND (title LIKE CONCAT('%', #{keyword}, '%') OR author LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
        </where>
    </sql>

    <select id="findAll" resultType="com.bookeatinglion.api.domain.Book">
        SELECT *
        FROM Books
        <include refid="bookConditions"/>
        ORDER BY book_id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*)
        FROM Books
        <include refid="bookConditions"/>
    </select>

    <select id="findById" resultType="com.bookeatinglion.api.domain.Book">
        SELECT *
        FROM Books
        WHERE book_id = #{bookId} AND sale_status = 'ON_SALE'
    </select>

    <update id="decreaseStock">
        UPDATE Books
        SET stock = stock - #{quantity}
        WHERE book_id = #{bookId} AND stock &gt;= #{quantity}
    </update>

    <update id="increaseStock">
        UPDATE Books
        SET stock = stock + #{quantity}
        WHERE book_id = #{bookId}
    </update>

</mapper>
