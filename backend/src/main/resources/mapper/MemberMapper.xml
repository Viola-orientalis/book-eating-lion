<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bookeatinglion.api.mapper.MemberMapper">

    <select id="findByUsername" resultType="com.bookeatinglion.api.domain.Member">
        SELECT member_id as memberId,
               username,
               password,
               name,
               role,
               is_deleted as isDeleted,
               deleted_at as deletedAt,
               created_at as createdAt,
               updated_at as updatedAt
        FROM Members
        WHERE username = #{username} AND is_deleted = 0
    </select>

    <insert id="insertMember" useGeneratedKeys="true" keyProperty="memberId">
        INSERT INTO Members (username, password, name, role)
        VALUES (#{username}, #{password}, #{name}, #{role})
    </insert>

    <update id="softDeleteMember">
        UPDATE Members
        SET is_deleted = 1, deleted_at = CURRENT_TIMESTAMP
        WHERE member_id = #{memberId}
    </update>

</mapper>
