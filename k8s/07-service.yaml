# apiVersion: v1
# kind: Service
# metadata:
#   name: mvc-app-service
#   namespace: prod
# spec:
#   type: NodePort
#   selector:
#     app: mvc-app
#   ports:
#   - port: 80
#     targetPort: 8080
#     protocol: TCP

# 로컬용
apiVersion: v1
kind: Service
metadata:
  name: springboot-service
  namespace: lion-app
spec:
  selector:
    app: springboot
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30090
  type: NodePort